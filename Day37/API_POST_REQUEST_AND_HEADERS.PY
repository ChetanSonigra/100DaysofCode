# GET - requests.get()  - we get the data
# POST -requests.post() - we give the data
# PUT - requests.put()
# DELETE - requests.delete()


# HEADERS: - Important metadata
# parameters will be visible in request url.
# so, secret keys will be visible in request.
# Better way is toto provide authentication through headers.

